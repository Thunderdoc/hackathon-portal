import API_URL from '../config';
import React, { useState, useEffect } from 'react'; import axios from 'axios'; import { Trophy, Medal, Award, Lock } from 'lucide-react'; import { motion, AnimatePresence } from 'framer-motion';  const Leaderboard = () => {     const [teams, setTeams] = useState([]);     const [visible, setVisible] = useState(false);      useEffect(() => {         const checkVisibility = async () => {             try {                 const res = await axios.get(`${API_URL}/api/config/results`);                 setVisible(res.data.released);             } catch (e) { }         };          const fetchLeaderboard = async () => {             // Fetch visibility first             try {                 const visRes = await axios.get(`${API_URL}/api/config/results`);                 const isVisible = visRes.data.released;                 setVisible(isVisible);                  if (isVisible) {                     const res = await axios.get(`${API_URL}/api/leaderboard`);                     setTeams(res.data);                 }             } catch (e) { }         };          checkVisibility();         fetchLeaderboard();         const interval = setInterval(fetchLeaderboard, 5000); // Live updates         return () => clearInterval(interval);     }, []);      const getIcon = (index) => {         if (index === 0) return <Trophy size={40} color="#ffd700" />;         if (index === 1) return <Medal size={40} color="#c0c0c0" />;         if (index === 2) return <Award size={40} color="#cd7f32" />;         return <span style={{ fontSize: '1.5rem', fontWeight: 'bold', color: '#666', width: '40px', textAlign: 'center' }}>{index + 1}</span>;     };      if (!visible) {         return (             <div style={{ minHeight: '100vh', background: '#0a0a0a', color: 'white', display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center', fontFamily: "'Montserrat', sans-serif" }}>                 <motion.div                     initial={{ opacity: 0, scale: 0.8 }}                     animate={{ opacity: 1, scale: 1 }}                     transition={{ duration: 0.5 }}                     style={{ textAlign: 'center' }}                 >                     <Lock size={80} color="#e63946" style={{ marginBottom: '2rem' }} />                     <h1 style={{ fontSize: '3rem', letterSpacing: '5px', marginBottom: '1rem' }}>RESULTS HIDDEN</h1>                     <p style={{ color: '#888', letterSpacing: '2px' }}>THE JUDGES ARE DELIBERATING...</p>                 </motion.div>             </div>         );     }      return (         <div style={{ minHeight: '100vh', background: '#0a0a0a', color: 'white', fontFamily: "'Montserrat', sans-serif", padding: '2rem' }}>             <h1 style={{ textAlign: 'center', fontSize: '4rem', marginBottom: '3rem', textTransform: 'uppercase', letterSpacing: '5px', textShadow: '0 0 20px rgba(76, 201, 240, 0.5)' }}>                 Live Standings             </h1>              <div style={{ maxWidth: '1000px', margin: '0 auto', display: 'flex', flexDirection: 'column', gap: '1.5rem' }}>                 <AnimatePresence>                     {teams.map((team, index) => (                         <motion.div                             key={team.id}                             initial={{ opacity: 0, y: 50 }}                             animate={{ opacity: 1, y: 0 }}                             transition={{ delay: index * 0.1 }}                             layout                             style={{                                 display: 'flex',                                 alignItems: 'center',                                 background: index === 0 ? 'linear-gradient(90deg, rgba(255, 215, 0, 0.2) 0%, rgba(0,0,0,0) 100%)' : 'rgba(255,255,255,0.05)',                                 padding: '1.5rem 2rem',                                 borderRadius: '15px',                                 border: '1px solid rgba(255,255,255,0.1)',                                 boxShadow: index === 0 ? '0 0 30px rgba(255, 215, 0, 0.1)' : 'none'                             }}                         >                             <div style={{ marginRight: '2rem' }}>                                 {getIcon(index)}                             </div>                             <div style={{ flex: 1 }}>                                 <h2 style={{ margin: 0, fontSize: '2rem', color: index === 0 ? '#ffd700' : 'white' }}>{team.teamName}</h2>                                 <p style={{ margin: 0, color: '#6c757d' }}>ID: {team.id}</p>                             </div>                             <div style={{ fontSize: '3rem', fontWeight: '900', color: index === 0 ? '#ffd700' : '#4cc9f0' }}>                                 {team.score} <span style={{ fontSize: '1rem', fontWeight: 'normal' }}>PTS</span>                             </div>                         </motion.div>                     ))}                 </AnimatePresence>             </div>         </div>     ); };  export default Leaderboard;
