import React, { useState } from 'react'; import axios from 'axios'; import { useNavigate } from 'react-router-dom'; import { Gavel } from 'lucide-react';  const JudgeLogin = () => {     const [username, setUsername] = useState('');     const [password, setPassword] = useState('');     const navigate = useNavigate();      const handleLogin = async (e) => {         e.preventDefault();         try {             const res = await axios.post(`${API_URL}/api/judge/login`, { username, password });             localStorage.setItem('judgeUser', JSON.stringify(res.data.judge));             navigate('/judge/dashboard');         } catch (err) {             alert('Invalid Credentials');         }     };      return (         <div style={{ minHeight: '100vh', display: 'flex', justifyContent: 'center', alignItems: 'center', background: '#0a0a0a', color: 'white', fontFamily: "'Montserrat', sans-serif" }}>             <form onSubmit={handleLogin} style={{ background: 'rgba(255,255,255,0.05)', padding: '3rem', borderRadius: '15px', border: '1px solid #333', textAlign: 'center', width: '350px' }}>                 <Gavel size={48} color="#fca311" style={{ marginBottom: '1rem' }} />                 <h2 style={{ marginBottom: '2rem', color: '#fca311' }}>JUDGE ACCESS</h2>                 <input                     type="text"                     placeholder="Username"                     value={username}                     onChange={e => setUsername(e.target.value)}                     style={{ width: '100%', padding: '1rem', marginBottom: '1rem', background: '#222', border: '1px solid #444', color: 'white', borderRadius: '5px' }}                 />                 <input                     type="password"                     placeholder="Password"                     value={password}                     onChange={e => setPassword(e.target.value)}                     style={{ width: '100%', padding: '1rem', marginBottom: '2rem', background: '#222', border: '1px solid #444', color: 'white', borderRadius: '5px' }}                 />                 <button type="submit" style={{ width: '100%', padding: '1rem', background: '#fca311', color: 'black', border: 'none', borderRadius: '5px', fontWeight: 'bold', cursor: 'pointer' }}>                     ENTER VISUAL INTERFACE                 </button>             </form>         </div>     ); };  export default JudgeLogin;
import API_URL from '../config';
